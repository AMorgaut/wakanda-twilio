/** * EXPERIMENTAL * * @module Twillio/core/httpRequestHandler * @author alexandre.morgaut@gmail.com **//** * @private * @method Twilio_handleRequest * @param {HTTPRequest} request * @param {HTTPResponse} response **/function Twilio_handleRequest(request, response) {	var		twimlId,		twiml,		twilioStore;	twimlId = request.path.split('/').pop();	storage.lock();	twilioStore = storage.twilio;	twiml = twilioStore.twilmlList[twimlId];	delete twilioStore.twilmlList[twimlId];	storage.twilio = twilioStore;	storage.unlock();	response.contentType = 'text/xml';	response.body = twiml;}